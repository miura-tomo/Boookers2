<h2>New book</h2>

<%= form_for(@book) do |f| %>
<div class="field">
	<label>Title</label>
	<%= f.text_field :title %>
</div>

<div class="field">
	<label>Opinion</label>
	<%= f.text_area :body %>
</div>

<div class="actions">
	<%= f.submit "Create Book" %>

<% end %>